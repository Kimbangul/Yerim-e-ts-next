import AboutView from '@/component/layout/about/AboutView';
import { link } from '@/util/data';

const AboutContainer = () => {
  return <AboutView link={link.resume} />;
};

export default AboutContainer;
